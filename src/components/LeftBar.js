import { makeStyles } from "@material-ui/core";

const st=makeStyles((theme)=>(
  {
   
  }
))

const LeftBar=()=> {
  const classes=st();
  return (
    <div >
     
    </div>
  
  );
}

export default LeftBar;
